import React, { PropTypes } from 'react'

function Mailchimp({ children, sectionName }) {
  return (
    <div className="mt4 mb4">
      <p>Mailchimp newsletter form TK</p>
    </div>
  )
}

Mailchimp.propTypes = {
}

export default Mailchimp
