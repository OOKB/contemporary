import React, { PropTypes } from 'react'

import Person from './Person'

function People({ }) {
  return (
    <div>
      <Person />
    </div>
  )
}

People.propTypes = {
}
People.defaultProps = {
}

export default People
